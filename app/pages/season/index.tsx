import React from 'react'

interface Props {
    className?: string,
    
}

export const SeasonPage: React.FC<Props> = ({ className }) => {
    return (
        <div>
            
        </div>
    )
}
